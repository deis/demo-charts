apiVersion: v1
kind: Service
metadata:
  name: guestbook-app
  labels:
    app: guestbook-app
    provider: guestbook
    heritage: helm
spec:
  type: LoadBalancer
  ports:
    - port: 80
  selector:
    app: guestbook-app
